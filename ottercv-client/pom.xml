<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>svkreml.ottercv</groupId>
        <artifactId>ottercv</artifactId>
        <version>1.1.1</version>
    </parent>

    <artifactId>ottercv-client</artifactId>
    <packaging>jar</packaging>
    <name>OtterCV Client</name>
    <description>Core application module for Otter Certificate Viewer</description>

    <build>
        <finalName>${app.finalName}-${project.version}</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>${maven-dependency-plugin.version}</version>
                <executions>
                    <!-- Class path in manifest format -->
                    <execution>
                        <id>build-manifest-classpath</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>build-classpath</goal>
                        </goals>
                        <configuration>
                            <includeScope>runtime</includeScope>
                            <fileSeparator>/</fileSeparator>
                            <outputProperty>manifest.dependency.classpath</outputProperty>
                            <prefix>${app.dependencyDirectory}</prefix>
                            <pathSeparator>;</pathSeparator>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>${build-helper-maven-plugin.version}</version>
                <executions>
                    <execution>
                        <id>regex-property</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>regex-property</goal>
                        </goals>
                        <configuration>
                            <name>manifest.dependency.replaced.classpath</name>
                            <value>${manifest.dependency.classpath}</value>
                            <regex>;</regex>
                            <replacement xml:space="preserve"> </replacement>
                            <failIfNoMatch>false</failIfNoMatch>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>${maven-jar-plugin.version}</version>
                <configuration>
                    <archive>
                        <addMavenDescriptor>false</addMavenDescriptor>
                        <manifest>
                            <mainClass>${app.mainClass}</mainClass>
                        </manifest>
                        <manifestEntries>
                            <Built-By>${project.organization.name}</Built-By>
                            <Class-Path>${manifest.dependency.replaced.classpath}</Class-Path>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
<plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.2.5</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                <version>${maven-resources-plugin.version}</version>
                <executions>
                    <execution>
                        <!-- Copying license -->
                        <id>copy-license</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.basedir}/target</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${project.basedir}/../ottercv-distrib</directory>
                                    <filtering>true</filtering>
                                    <includes>
                                        <include>license.txt</include>
                                    </includes>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-graphics</artifactId>
            <version>21</version>
            <classifier>linux</classifier>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>21</version>
            <classifier>linux</classifier>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-base</artifactId>
            <version>21</version>
            <classifier>linux</classifier>
        </dependency>
        <dependency>
            <groupId>jakarta.xml.bind</groupId>
            <artifactId>jakarta.xml.bind-api</artifactId>
            <version>4.0.2</version>
        </dependency>
        <dependency>
            <groupId>com.sun.xml.bind</groupId>
            <artifactId>jaxb-impl</artifactId>
            <version>4.0.0</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcpkix-jdk18on</artifactId>
            <version>1.77</version>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.2</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.26.0</version>
            <scope>test</scope>
        </dependency>

        <!-- Logging -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>jcl-over-slf4j</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>jul-to-slf4j</artifactId>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>unix</id>
            <activation>
                <os>
                    <family>unix</family>
                </os>
            </activation>
            <properties>
                <javapackager.name>${app.finalName}</javapackager.name>
                <javapackager.createTarball>true</javapackager.createTarball>
                <copy-rename.goal>copy</copy-rename.goal>
                <copy-rename.sourceFile>${project.basedir}/target/${javapackager.name}-${project.version}-linux.tar.gz</copy-rename.sourceFile>
                <copy-rename.destinationFile>${project.basedir}/target/${app.finalName}-${project.version}.tar.gz</copy-rename.destinationFile>
            </properties>
        </profile>
        <profile>
            <id>linux-appimage</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <version>${maven-dependency-plugin.version}</version>
                        <executions>
                            <execution>
                                <id>copy-dependencies-for-appimage</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>copy-dependencies</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${project.build.directory}/libs</outputDirectory>
                                    <includeScope>runtime</includeScope>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>download-appimagetool</id>
                                <phase>generate-resources</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>sh</executable>
                                    <arguments>
                                        <argument>-c</argument>
                                        <argument><![CDATA[mkdir -p "${project.build.directory}" && ARCH=$(uname -m); if [ "$ARCH" = "x86_64" ]; then URL="https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-x86_64.AppImage"; elif [ "$ARCH" = "aarch64" ]; then URL="https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-aarch64.AppImage"; else echo "Unsupported architecture: $ARCH"; exit 1; fi; curl -L -o "${project.build.directory}/appimagetool-${ARCH}.AppImage" "$URL" && chmod +x "${project.build.directory}/appimagetool-${ARCH}.AppImage" && cd "${project.build.directory}" && rm -rf squashfs-root && "./appimagetool-${ARCH}.AppImage" --appimage-extract]]></argument>
                                    </arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>make-appimage-script-executable</id>
                                <phase>generate-resources</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>chmod</executable>
                                    <arguments>
                                        <argument>+x</argument>
                                        <argument>${project.basedir}/../ottercv-distrib/appimage/build-appimage.sh</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>build-jre</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>sh</executable>
                                    <arguments>
                                        <argument>-c</argument>
                                        <argument><![CDATA[${env.JAVA_HOME}/bin/jlink --module-path ${env.JAVA_HOME}/jmods --add-modules java.base,java.desktop,java.logging,java.xml,java.sql,java.naming,java.compiler,jdk.unsupported --output ${project.build.directory}/jre --no-header-files --no-man-pages --strip-debug --compress=2]]></argument>
                                    </arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>build-appdir</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>${project.basedir}/../ottercv-distrib/appimage/build-appimage.sh</executable>
                                    <arguments>
                                        <argument>${project.build.directory}</argument>
                                        <argument>${project.build.directory}/OtterCV.AppDir</argument>
                                        <argument>${project.basedir}/assets</argument>
                                        <argument>${project.build.directory}/jre</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>create-appimage</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>sh</executable>
                                    <arguments>
                                        <argument>-c</argument>
                                        <argument><![CDATA["${project.build.directory}/squashfs-root/AppRun" "${project.build.directory}/OtterCV.AppDir" "${project.build.directory}/ottercv-${project.version}-$(uname -m).AppImage"]]></argument>
                                    </arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>cleanup-appimagetool</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>rm</executable>
                                    <arguments>
                                        <argument>-rf</argument>
                                        <argument>${project.build.directory}/squashfs-root</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        <execution>
                                <id>test-appimage</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>sh</executable>
                                    <arguments>
                                        <argument>-c</argument>
                                        <argument><![CDATA[if [ "${AI_TEST}" = "true" ]; then chmod +x "${project.build.directory}/ottercv-${project.version}-$(uname -m).AppImage" && cd "${project.build.directory}" && "./ottercv-${project.version}-$(uname -m).AppImage" --appimage-extract && timeout 10 xvfb-run "./squashfs-root/AppRun" || true; rm -rf squashfs-root; fi]]></argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>native-deploy</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>io.github.fvarrui</groupId>
                        <artifactId>javapackager</artifactId>
                        <version>${javapackager.version}</version>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>package</goal>
                                </goals>
                                <configuration>
                                    <additionalResources>
                                        <additionalResource>${project.basedir}/../ottercv-distrib/readme.txt</additionalResource>
                                        <additionalResource>${project.basedir}/../ottercv-distrib/ottercv.l4j.ini</additionalResource>
                                    </additionalResources>
                                    <assetsDir>${project.basedir}/assets</assetsDir>
                                    <bundleJre>true</bundleJre>
                                    <createTarball>${javapackager.createTarball}</createTarball>
                                    <displayName>${app.fullName}</displayName>
                                    <licenseFile>${project.basedir}/target/license.txt</licenseFile>
                                    <mainClass>${app.mainClass}</mainClass>
                                    <name>${javapackager.name}</name>
                                    <organizationName>${project.organization.name}</organizationName>
                                    <organizationUrl>${project.organization.url}</organizationUrl>
                                    <version>${project.version}</version>
                                    <linuxConfig>
                                        <pngFile>${project.basedir}/assets/linux/${app.finalName}.png</pngFile>
                                        <generateAppImage>false</generateAppImage>
                                    </linuxConfig>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>com.coderplus.maven.plugins</groupId>
                        <artifactId>copy-rename-maven-plugin</artifactId>
                        <version>${copy-rename-maven-plugin.version}</version>
                        <executions>
                            <execution>
                                <id>rename-file</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>${copy-rename.goal}</goal>
                                </goals>
                                <configuration>
                                    <sourceFile>${copy-rename.sourceFile}</sourceFile>
                                    <destinationFile>${copy-rename.destinationFile}</destinationFile>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
